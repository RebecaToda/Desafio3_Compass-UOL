import styled from "styled-components";

const PageLocation = styled.div`
  display: inline-flex;
  gap: 6px;
`;

const Components = {
  BackgroundSection,
  PageLocation,
  Link,
};

export default Components;
